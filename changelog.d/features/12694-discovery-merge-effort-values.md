- **feat(providers):** parse Merge Gateway's per-vendor-route `effort_values` (nested under `vendors.<vendor>.capabilities.reasoning` in `/v1/models`) into the synced `supportedThinkingEfforts` vocabulary, intersected across vendor routes so a synced level is always honored on every route the model can land on; the intersected vocabulary's highest tier becomes the `defaultThinkingEffort` fallback — re-syncing a Merge connection no longer wipes operator effort data (GLM-5.3-flash now exposes its native `low|high|max` without a manual DB patch)
